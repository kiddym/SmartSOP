-r requirements.txt

# 测试
pytest>=8.3,<9.0
pytest-cov>=6.0,<7.0
pytest-asyncio>=0.24,<1.0
httpx>=0.27,<1.0

# Lint / format / type
# 小版本钉死：ruff/mypy 跨小版本会改格式化风格与 strict 检查面，宽松 pin 曾导致
# 解析到远新版本、整库门禁红（见提交历史）。锁定小版本线、仅放行补丁。
ruff>=0.15.14,<0.16
mypy>=1.20,<1.21

# 安全扫描
bandit>=1.7,<2.0

# 开发工具
ipython>=8.29,<9.0
pre-commit>=4.0,<5.0
